// 4. Function with no arguments and return type
#include <stdio.h>

int getNumber() {
    return 42;
}

int main() {
    int n = getNumber();
    printf("Number returned = %d\n", n);
    return 0;
}

